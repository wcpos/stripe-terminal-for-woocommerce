# Blocks payment method frontend

Builds the WooCommerce Blocks payment method registration script for Stripe Terminal.

## Commands

```bash
npm install
npm run build
```

Output is written to `assets/js/blocks/` (`index.js` + `index.asset.php`) and should be committed so plugin zips work without a build step.
